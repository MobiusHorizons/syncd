language: c
sudo: false

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
    - libltdl-dev
    - libjson0-dev

#install:
#  - libtoolize -c
#  - cd libltdl
#  - ./configure
#  - make && make install

script:
  - ls /usr/lib/*-linux-gnu
  - mkdir -p build
  - cd build
  - cmake ..
  - make
  - make install

#script: ./autogen.sh && ./configure && make
