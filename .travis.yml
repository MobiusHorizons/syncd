language: c

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
    - libltdl-dev
  
script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make
  - sudo make install

#script: ./autogen.sh && ./configure && make 
